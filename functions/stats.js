/* 
1. deberia crear escenarios de entrada de options
caso 1. validate (listo)
caso 2. stats 
caso 3. validate y stats

caso stats.
inicializar los 
links total en 0.
links unique en 0.

if(options === "--s" || options === "--stat"){
    .length para contabilizar el total de links.
    .filter solo dejar los unicos y luego .length para contabilizar los links unicos.

    reporte de contador de links.
    
    buscar un cominio que no exist para psar un modelo de broken. 
    



options = { validate: process.argv.includes('--v') || process.argv.includes('--validate')}



if (process.argv.includes('--v') || process.argv.includes('--validate')) {
    return true
}
options = { validate: true} 
}
else{
options = { validate: false} 
}






*/ 